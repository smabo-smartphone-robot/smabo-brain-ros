# Placeholder 10 m x 10 m empty map (all free) so Nav2 + AMCL can start before
# a real map is built. Replace with a slam_toolbox-saved map for real use.
image: empty_map.pgm
mode: trinary
resolution: 0.05
origin: [-5.0, -5.0, 0.0]
negate: 0
occupied_thresh: 0.65
free_thresh: 0.25
